The program currently does not support command line arguments. The default program create two lanes, one intersection and 20 vehicles. To use different parameters it must be adjusted programatically.

To run:
python Main.py